import Box from '@mui/material/Box';
import React from 'react';
import ModelPageContainer from 'views/admin/modelPage/ModelPageContainer';

const AdminMain = () => (
  <>
    <Box sx={{ backgroundColor: '#FFFAFD' }}>
      <ModelPageContainer />
    </Box>
  </>
);

export default AdminMain;
