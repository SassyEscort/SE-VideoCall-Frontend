import { Box, styled } from '@mui/material';

export const HomePageMainContainer = styled(Box)(() => ({
  display: 'flex',
  flexDirection: 'column',
  paddingLeft: '15px',
  paddingRight: '15px'
}));
